interface:
  display_name: "Detangle Codebase"
  short_description: "Review-first cleanup for tangled codebases"
  default_prompt: "Use $detangle to review this tangled repository and recommend a safe architecture cleanup plan."
