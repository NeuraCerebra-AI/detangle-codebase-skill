interface:
  display_name: "Detangle Skill"
  short_description: "Review and simplify tangled repository architecture"
  default_prompt: "Use $architecture-transform in execute mode to review this repository, select a bounded architecture cleanup plan, and implement it within the skill's safety boundaries."
